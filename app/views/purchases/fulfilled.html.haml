%hr.long-hr
%p=t '.heading_count_teaser', :count => @purchases_count

=render 'purchase_listing_table', 
  :purchases => @purchases, 
  :columns => [:item_with_notes, :purchaser, :quantity_purchased, :total_price, :item_sent_at]
